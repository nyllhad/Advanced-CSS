function toggleNav() {
             var updateElement = document.getElementById("list");
          //toggle adds a class if it's not there or removes it if it is.
          updateElement.classList.toggle("open");
      }   

  function barSwitch(x) {
    x.classList.toggle("bar-switch");
  }
